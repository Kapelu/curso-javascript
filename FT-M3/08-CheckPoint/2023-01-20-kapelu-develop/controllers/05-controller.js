const utils = require('../utils');
/*⚠️ No modificar nada arriba de esta línea ⚠️

  5️⃣ ***** EJERCICIO 5 ***** - obtenerCiudadesDelCodigoPostal 5️⃣:
  
  🟢 Retornar un array de ciudades que pertenezcan al código postal solicitado.
  📌 Si el código postal solicitado no existe, arrojar un error que diga: "Codigo postal no encontrado".
  📌 Si el código postal no tiene ciudades, retorna un string que diga: "No se encontraron ciudades asociadas".
  

    📢 PUNTOS A TENER EN CUENTA 📢
  1) Recuerda que el mensaje de error deben ser exactamente como lo pide el enunciado.
  2) Dentro de cada código postal está guardada la información de cada ciudad correspondiente.
*/

const obtenerCiudadesDelCodigoPostal = (codigoPostal) => {
  
};

// ⚠️ No modificar nada debajo de esta línea ⚠️
module.exports = obtenerCiudadesDelCodigoPostal;
